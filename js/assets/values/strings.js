'use strict';

import LocalizedStrings from 'react-native-localization';

export let string = new LocalizedStrings({
   en:{
       app_name:'AppName',
       // 退出页面
       toast_confirm_exit:'Press again to exit',
   }
});